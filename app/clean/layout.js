// app/cleanpage/layout.js

import CustomLayout from '../CustomLayout';

export default function CleanPageLayout({ children }) {
  return (
  
  <CustomLayout>{children}</CustomLayout>)
  
//   <CustomLayout>{children}</CustomLayout>;
}
